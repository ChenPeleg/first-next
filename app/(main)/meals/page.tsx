export default async function  MealsPage (props :  Record<string, any>) {
    const params =  await props.params;

    return <div className={'bg-amber-200 w-full h-full'}><h1> Meals page</h1></div>
}
